# 🍽️ Munchly - Quick Start Guide

## ✨ Что реализовано

### ✅ Полный функционал
1. **Аутентификация**
   - Регистрация с валидацией (email, password, name, phone)
   - Вход в систему
   - Сохранение сессии

2. **Рестораны**
   - Список из 6 ресторанов с разными кухнями
   - Поиск по названию и кухне
   - Фильтрация по типу кухни
   - Детальная информация о ресторане
   - Список доступных столиков

3. **Бронирование**
   - Выбор столика из доступных
   - Форма бронирования (дата, время, количество гостей)
   - Подтверждение бронирования
   - Просмотр своих бронирований

### 🎨 Дизайн
- **Оранжево-белая палитра**
  - Primary: `#FF6B35` (яркий оранжевый)
  - Secondary: `#FFB84D` (золотисто-оранжевый)
  - Background: `#FFFBF5` (теплый белый)
  - Accent: `#FFF4E6` (кремовый)
- Современный UI с карточками, тенями и скругленными углами
- Градиенты и плавные анимации
- Material Design 3

## 🚀 Быстрый запуск

### 1. Запустите приложение

```bash
flutter run
```

### 2. Войдите в систему

На экране логина используйте тестовые учетные данные:

**Аккаунт 1:**
- Email: `test@munchly.com`
- Password: `password123`

**Аккаунт 2:**
- Email: `demo@munchly.com`
- Password: `demo123`

Или нажмите кнопку 📋 рядом с учетными данными для автозаполнения.

### 3. Исследуйте рестораны

После входа вы увидите список из 6 ресторанов:
- 🇮🇹 La Bella Italia (Italian, $$$)
- 🇯🇵 Sushi Master (Japanese, $$$$)
- 🇺🇸 The Burger Joint (American, $$)
- 🇮🇳 Spice Garden (Indian, $$)
- 🇫🇷 Le Petit Bistro (French, $$$$)
- 🇲🇽 Taco Fiesta (Mexican, $)

**Возможности:**
- Используйте поиск для фильтрации
- Выбирайте кухню через чипсы
- Нажмите на карточку ресторана для подробностей

### 4. Забронируйте столик

1. Выберите ресторан
2. Посмотрите доступные столики
3. Нажмите на столик для выбора
4. Нажмите "Continue to Booking"
5. Заполните форму:
   - Выберите дату
   - Выберите время
   - Укажите количество гостей
6. Нажмите "Confirm Booking"
7. Получите подтверждение с номером брони

### 5. Просмотрите бронирования

Нажмите на FAB "My Bookings" на главном экране.

## 📦 Мок данные

Приложение работает в **mock режиме** без реального backend:

### Тестовые пользователи
```dart
test@munchly.com / password123
demo@munchly.com / demo123
```

### 6 ресторанов с данными
Каждый ресторан имеет:
- Название, описание, кухню
- Рейтинг и количество отзывов
- Адрес, телефон, часы работы
- 3-5 столиков разной вместимости
- Статус (открыт/закрыт)

## 🗄️ MongoDB настройка

### Текущая конфигурация
```
mongodb://localhost:27017/munchly
```

### Чтобы подключить реальный backend:

1. **Измените режим в** `lib/core/config/app_config.dart`:
```dart
static const bool useMockData = false; // Выключить mock
```

2. **Настройте URL в** `lib/core/config/api_config.dart`:
```dart
static const String baseUrl = 'http://localhost:3000/api';
```

3. **Backend должен предоставлять endpoints:**

**Auth:**
- `POST /api/auth/login`
- `POST /api/auth/register`
- `POST /api/auth/logout`
- `GET /api/auth/me`

**Restaurants:**
- `GET /api/restaurants`
- `GET /api/restaurants/:id`

**Bookings:**
- `POST /api/bookings`
- `GET /api/bookings/my`
- `DELETE /api/bookings/:id`

### Пример структуры MongoDB

**Users Collection:**
```json
{
  "_id": "ObjectId",
  "email": "user@example.com",
  "password": "hashed_password",
  "name": "John Doe",
  "phone": "+1234567890",
  "createdAt": "ISODate"
}
```

**Restaurants Collection:**
```json
{
  "_id": "ObjectId",
  "name": "La Bella Italia",
  "description": "...",
  "cuisine": "Italian",
  "address": "123 Main St",
  "phone": "+1234567890",
  "rating": 4.8,
  "reviewCount": 342,
  "priceRange": "$$$",
  "isOpen": true,
  "tables": [
    {
      "id": "t1",
      "number": 1,
      "capacity": 2,
      "location": "window"
    }
  ]
}
```

**Bookings Collection:**
```json
{
  "_id": "ObjectId",
  "userId": "user_object_id",
  "restaurantId": "restaurant_object_id",
  "tableId": "t1",
  "date": "2024-01-30",
  "time": "19:00",
  "guests": 2,
  "status": "confirmed",
  "createdAt": "ISODate"
}
```

## 🛠️ Разработка

### Структура проекта

```
lib/
├── core/
│   ├── config/          # API, Router, DI, App Config
│   ├── constants/       # Константы приложения
│   ├── error/           # Обработка ошибок
│   ├── network/         # Dio HTTP client
│   ├── theme/           # Оранжевая тема
│   └── utils/           # Валидаторы
│
└── features/
    ├── auth/           # Аутентификация
    │   ├── data/       # Models, DataSources, Repositories
    │   ├── domain/     # Entities, UseCases
    │   └── presentation/ # Providers, Screens, Widgets
    │
    ├── restaurants/    # Рестораны
    │   └── [same structure]
    │
    └── bookings/       # Бронирования
        └── [same structure]
```

### Clean Architecture

- **Domain Layer**: Бизнес-логика, entities, use cases
- **Data Layer**: Модели, API calls, repositories
- **Presentation Layer**: UI, state management (Provider)

### Добавление новых ресторанов

Редактируйте `lib/features/restaurants/data/datasources/restaurants_mock_datasource.dart`:

```dart
_restaurants.addAll([
  Restaurant(
    id: '7',
    name: 'Your Restaurant',
    // ... rest of fields
  ),
]);
```

## 🎨 Кастомизация темы

Откройте `lib/core/theme/app_theme.dart` и измените цвета:

```dart
static const Color primaryColor = Color(0xFFFF6B35);  // Ваш цвет
static const Color secondaryColor = Color(0xFFFFB84D); // Ваш цвет
```

## 📱 Тестирование

```bash
# Анализ кода
flutter analyze

# Запуск тестов
flutter test

# Проверка форматирования
flutter format .
```

## 🐛 Решение проблем

### Проблема: "Не могу войти"
- Убедитесь, что используете правильные credentials
- Проверьте, что `AppConfig.useMockData = true`
- Перезапустите приложение

### Проблема: "Пустой список ресторанов"
- Проверьте, что mock data загружается
- Посмотрите консоль на наличие ошибок
- Перезапустите приложение

### Проблема: "Ошибка при бронировании"
- Проверьте, что выбрали дату и время
- Убедитесь, что количество гостей не превышает вместимость столика
- Проверьте консоль на наличие ошибок

## 📚 Дополнительная информация

- **Документация**: См. [ARCHITECTURE.md](ARCHITECTURE.md)
- **Mock Mode**: См. [MOCK_MODE.md](MOCK_MODE.md)
- **Auth Module**: См. [lib/features/auth/README.md](lib/features/auth/README.md)

## 🎯 Следующие шаги

1. ✅ **Готово**: Аутентификация, рестораны, бронирования
2. 🚧 **Можно добавить**:
   - Интерактивная карта столиков (Leaflet/Flutter Map)
   - Отмена и редактирование бронирований
   - История бронирований
   - Избранные рестораны
   - Отзывы и рейтинги
   - Push уведомления
   - Профиль пользователя

## 💡 Совет

Сначала протестируйте все в mock режиме, убедитесь что UI работает как нужно, а затем подключайте real backend с MongoDB!

---

**Приятного использования Munchly! 🍽️✨**
