# Munchly 🍽️

Мобильное приложение для бронирования столиков в ресторанах с интерактивной мини-картой зала.

## Основные возможности

- ✅ Аутентификация (Email/Password)
- ✅ Просмотр списка ресторанов
- 🚧 Интерактивная карта зала ресторана
- 🚧 Бронирование столиков
- 🚧 Управление бронированиями

## Технологический стек

- **Frontend**: Flutter 3.7+
- **State Management**: Provider
- **Navigation**: GoRouter
- **HTTP Client**: Dio
- **Maps**: Flutter Map (Leaflet)
- **Storage**: SharedPreferences
- **Backend**: REST API
- **Database**: MongoDB Atlas

## Архитектура

Проект построен на принципах **Clean Architecture** с feature-based структурой.

Подробнее: [ARCHITECTURE.md](ARCHITECTURE.md)

## Установка и запуск

### Требования

- Flutter SDK 3.7.0 или выше
- Dart SDK 3.0.0 или выше

### Шаги установки

1. Клонируйте репозиторий
```bash
git clone <repository-url>
cd munchly
```

2. Установите зависимости
```bash
flutter pub get
```

3. Сгенерируйте код для JSON serialization
```bash
flutter pub run build_runner build --delete-conflicting-outputs
```

4. Настройте API endpoint
Откройте `lib/core/config/api_config.dart` и укажите URL вашего backend:
```dart
static const String baseUrl = 'https://your-api-url.com';
```

5. Запустите приложение
```bash
flutter run
```

## Структура проекта

```
lib/
├── core/                      # Общие компоненты
│   ├── config/               # Конфигурации (API, Router, DI)
│   ├── constants/            # Константы
│   ├── error/                # Обработка ошибок
│   ├── network/              # HTTP клиент
│   ├── theme/                # Темы приложения
│   └── utils/                # Утилиты
│
└── features/                  # Модули функциональности
    ├── auth/                  # Аутентификация
    ├── restaurants/           # Рестораны
    └── bookings/              # Бронирования
```

## Модули

### ✅ Authentication (Готово)
- Email/Password авторизация
- Регистрация пользователя
- Хранение токенов
- Валидация форм

Подробнее: [features/auth/README.md](lib/features/auth/README.md)

### 🚧 Restaurants (В разработке)
- Список ресторанов
- Детальная информация
- Интерактивная карта зала

### 🚧 Bookings (В разработке)
- Создание бронирования
- Список бронирований
- Отмена бронирования

## Запуск тестов

```bash
flutter test
```

## Генерация кода

При изменении моделей с JSON serialization запустите:

```bash
flutter pub run build_runner build --delete-conflicting-outputs
```

или для watch режима:

```bash
flutter pub run build_runner watch
```

## Анализ кода

```bash
flutter analyze
```

## Разработка

### Добавление новой фичи

1. Создайте структуру папок в `lib/features/your_feature/`
2. Реализуйте слои: domain → data → presentation
3. Добавьте зависимости в DI
4. Создайте роуты в router.dart

### Подключение к backend

API endpoints настраиваются в `lib/core/config/api_config.dart`

Формат ответа API должен быть:
```json
{
  "data": { ... },
  "message": "Success"
}
```

## Лицензия

MIT

## Контакты

Для вопросов и предложений создавайте issue в репозитории.
