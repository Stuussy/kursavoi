/// Application configuration
class AppConfig {
  /// Use mock data sources (for development without backend)
  static const bool useMockData = false;

  /// Debug mode
  static const bool debugMode = true;

  /// Show debug logs
  static const bool showLogs = true;
}
